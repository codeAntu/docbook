

export function sendVerificationCode(number: string, code: string) {
  // Integrate with an SMS service provider to send the code
  console.log(`Sending verification code ${code} to number ${number}`);
}